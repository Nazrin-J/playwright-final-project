# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: logininvaliduser.spec.js >> User login with invalid username
- Location: tests\logininvaliduser.spec.js:6:5

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: "Wrong password."
Received: "User does not exist."
```

# Test source

```ts
  1  | import{test,expect} from '@playwright/test'
  2  | //import {Loginvalid} from '../pages/Login-validcred'  
  3  | import{Loginvaliduser}from '../pages/Login-invaliduser'
  4  | 
  5  | 
  6  | test('User login with invalid username',async({page})=>{
  7  | const logininvaliduser=new Loginvaliduser(page)  //constructor invoking
  8  | await logininvaliduser.loginurl()
  9  | await logininvaliduser.loginbutton()
  10 | await logininvaliduser.validlogincred('dfgedgdg1','testdemoproj@123')
  11 | page.on('dialog',async dialog=>{             //listener creation -it listens for any pop up like alerts/popups,this step should create first if any pop up/alert appears-to handle the alerts/popups listeners are created first
> 12 | expect(dialog.message()).toBe('Wrong password.') //dialog.message()-actual text,we're checking the actual alert text with our expected text in toBe.verification-expectation-if any alerts come it will verifies our given message and compare it with our given message and alert message-and checks if both are same
     |                          ^ Error: expect(received).toBe(expected) // Object.is equality
  13 | await dialog.accept()  //to click OK button on an alert we use accept()
  14 |     })
  15 | await logininvaliduser.loginfailed()
  16 | })
```